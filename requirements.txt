# ComfyUI-ZhangyuAPI dependencies
# Note: numpy, Pillow, and torch are already provided by ComfyUI.
# Only install if running outside ComfyUI or if missing.

numpy>=1.20
Pillow>=9.0
httpx[http2]>=0.27
torch>=2.0
